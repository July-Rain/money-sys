<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lawschool.dao.practicecenter.PaperExerciseDao">

    <select id="findByUserAndConIds" resultType="com.lawschool.form.CommonForm">
        SELECT
            configure_id AS "key",
            status AS "value"
        FROM LAW_EXERCISE_PAPER
        <where>
            configure_id in
            <foreach collection="list" open="(" close=")" separator="," item="item">
                #{item}
            </foreach>
        </where>
    </select>

    <select id="getQuestAndAnswer" resultType="com.lawschool.form.QuestForm">
        SELECT
            t.id,
            t.com_content,
            t.question_difficulty,
            t.question_type,
            t.answer_id,
            t.legal_basis,
            t.answer_describle,
            t.answer_choice_number,
            r.ANSWER AS "userAnswer",
            r.right
        FROM law_testquestions t
        LEFT JOIN LAW_PAPER_RECORD r
            ON t.id = r.question_id
            AND r.create_user = #{userId}
            AND r.task_id = #{id}
        <where>
            t.id IN
            <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
        ORDER BY t.id

    </select>

    <insert id="insert">
        INSERT INTO LAW_EXERCISE_PAPER
        (
            id,
            CONFIGURE_ID,
            QUESTIONS,
            USER_ID,
            STATUS,
            NO,
            TOTAL
        ) VALUES (
            #{id},
            #{configureId},
            #{questions},
            #{userId},
            #{status},
            #{no},
            #{total}
        )
    </insert>

    <!-- 更新练习任务的整体答题情况 -->
    <update id="updateAnswerNum">
        UPDATE LAW_EXERCISE_PAPER
        SET ANSWER_NUM = ANSWER_NUM + #{num}
        <where>
            id = #{id}
        </where>
    </update>

    <update id="updateStatus">
        UPDATE LAW_EXERCISE_PAPER
        SET status = #{status}
        <where>
            id = #{id}
        </where>
    </update>


</mapper>