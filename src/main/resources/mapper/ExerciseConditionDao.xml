<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lawschool.dao.ExerciseConditionDao">

    <insert id="insert">
        INSERT INTO LAW_EXERCISE_CONDITION
        (
            id,
            topic_id,
            topic_name,
            primary_num,
            middle_num,
            senior_num，
            con_id

        ) VALUES (
            #{id},
            #{topicId},
            #{topicName},
            #{primaryNum},
            #{middleNum},
            #{seniorNum},
            #{conId}

        )
    </insert>

</mapper>